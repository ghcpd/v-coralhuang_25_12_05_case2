# Frontend — React + Vite

Start local dev server:

```powershell
cd frontend
npm install
npm run dev
```

The app expects a backend at http://localhost:4000 by default. Set VITE_API_BASE environment variable to override (e.g., VITE_API_BASE=http://localhost:4000/api).
